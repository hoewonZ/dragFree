# 发布历史（中文）

> **说明**
>
> - 各版本窗口与 `package.json` 的版本演进一致；**不列出** 单独的 `chore(release): bump version …` 提交。
> - **不列出** 仅修改 `RELEASE_HISTORY.md`、`COMMIT_HISTORY.md`、`AGENTS.md` 的维护性提交。
> - 下列版本窗口按 **新版本在上、旧版本在下（倒序）** 排列；每个版本内的「窗口内提交」按 **时间由旧到新** 排列。

## v0.5.1（自 v0.5.0）

- **版本变更**：`0.5.0 -> 0.5.1`（PATCH）。版本号在发布提交中与 `RELEASE_HISTORY.md` 一并更新（本说明不重复列出单独的版本号提升提交）。
- **窗口内提交摘要**（自 `1e428f3` 之后，不含仅维护 `RELEASE_HISTORY.md` / `COMMIT_HISTORY.md` / `AGENTS.md` 的提交）：
  - `023f1f1` fix(main)：首次启动（无 `config.json`）时按主显示器工作区居中初始化热区位置并持久化，避免标题栏溢出屏幕上沿。

## v0.5.0（自 v0.4.1）

- **版本变更**：`0.4.1 -> 0.5.0`（MINOR）。版本号在发布提交中与 `RELEASE_HISTORY.md` 一并更新（本说明不重复列出单独的版本号提升提交）。
- **窗口内提交摘要**（自 `c1029df` 之后，不含仅维护 `RELEASE_HISTORY.md` / `COMMIT_HISTORY.md` / `AGENTS.md` 的提交）：
  - `8fabef7` feat：更新版权声明与用户指南。
  - `ed59830` fix(quick-open)：快开弹窗高度与拖拽弹窗统一。
  - `383d483` chore(build)：打包元数据添加作者信息。
  - `1e428f3` feat(main)：便携版可写数据与 exe 同级 `config` 目录（`PORTABLE_EXECUTABLE_DIR` 优先），日志/背景/常用文件夹快捷方式一并迁入；首次启动可从旧版 `AppData` 下 `dragfree` 目录迁移。

## v0.4.1（自 v0.4.0）

- **版本变更**：`0.4.0 -> 0.4.1`（PATCH）。版本号在发布提交中与 `RELEASE_HISTORY.md` 一并更新（本说明不重复列出单独的版本号提升提交）。
- **窗口内提交摘要**（自 `6f28a44` 之后，不含仅维护 `RELEASE_HISTORY.md` / `COMMIT_HISTORY.md` / `AGENTS.md` 的提交）：
  - `c466364` fix(shortcut)：macOS 下模式切换快捷键改为 Control+空格。
  - `47333db` fix(main)：macOS/Linux 托盘使用多倍率图标，逻辑尺寸 16×16。
  - `1dd138a` fix(main)：快开/拖拽面板与新建文件夹窗统一 `screen-saver` 置顶，热区刷新后弹窗仍高于钉住热区。
  - `a3638e2` fix(new-folder)：加高新建文件夹窗口并支持滚动与长路径折行，避免底部按钮被裁切。

## v0.4.0（自 v0.3.0）

- **版本变更**：`0.3.0 -> 0.4.0`（MINOR）。版本号在发布提交中与 `RELEASE_HISTORY.md` 一并更新（本说明不重复列出单独的版本号提升提交）。
- **窗口内提交摘要**（自 `f15b29a` 之后，不含仅维护 `RELEASE_HISTORY.md` / `COMMIT_HISTORY.md` / `AGENTS.md` 的提交）：
  - `f9f33e1` 热区导出 Word（.docx）、配置目录 `favorite-links` 与 Windows 常用文件夹快捷方式同步、另存为热区所在屏；补充 `docs/user-guide.zh-CN.md`。

## v0.3.0（自 v0.2.0）

- **版本变更**：`0.2.0 -> 0.3.0`（MINOR）。
- **窗口内提交摘要**（自 `b5ab5db` 之后，不含仅维护 `RELEASE_HISTORY.md` / `COMMIT_HISTORY.md` / `AGENTS.md` 的提交）：
  - `f15b29a` feat(config)：热区背景图改为缩略图预览；叠放布局修复与图同显「未选择」；移除选择按钮与打开背景库，点击缩略图选图/替换。

## v0.2.0（自 v0.1.7）

- **版本变更**：`0.1.7 -> 0.2.0`（MINOR）。版本号在提交 `b5ab5db` 中提升（本发布说明不重复列出该 bump 提交）。
- **窗口内提交摘要**（自 `e8350f6` 之后，不含仅维护 `RELEASE_HISTORY.md` / `COMMIT_HISTORY.md` / `AGENTS.md` 的提交）：
  - `5a3a6e9` fix(config)：配置页添加/删除常用文件夹时不重复显示「已修改」。
  - `8200703` feat(config)：热区设置中展示文本加粗与颜色同一行；背景图导入保留安全文件名、选择器默认打开背景库、新增打开背景库文件夹；修复 `AGENTS.md` 正文编码。

## v0.1.7（自 v0.1.6）

- **版本变更**：`0.1.3 -> 0.1.7`（PATCH）。`package.json` 自 `0.1.3` 提升至 `0.1.7`（与文档化 v0.1.6 窗口衔接；本说明不写单独 bump 提交）。
- **窗口内提交摘要**（自 `f9519be` 之后，不含仅维护三份元文档的提交）：
  - `bddad82` 常用文件夹分行省略、拖拽会话与路径修正、单测补充。
  - `869fa65` 快开与面板共用列表/平铺及尺寸配置、配置文案同步。
  - `501f396` 设置顶栏固定、表单区独立滚动。
  - `6f837f5` 常用文件夹拖动手柄排序、边缘滚动与提示、「已修改」/「已删除」提示。

## v0.1.6（自 v0.1.3）

- **版本变更**：`0.1.3 -> 0.1.6`（MINOR）。文档化功能窗口止于 `f9519be`；`package.json` 在 v0.1.7 发布时一并从 `0.1.3` 升至 `0.1.7`。
- **窗口内提交摘要**（不含仅维护 `RELEASE_HISTORY.md` / `COMMIT_HISTORY.md` / `AGENTS.md` 的提交）：
  - `9422abe` fix(overlay)：编辑态拖拽标题时降低 preview IPC 频率，结束时补发一次以同步最终位置。
  - `4649d5e` feat(overlay)：允许热区底边越过屏幕下沿同时保持标题栏可操作；跨屏按钮图标语义优化；编辑文本时 Tab 插入制表符而非切换焦点。
  - `1326dbd` feat(main)：拖拽面板与快开弹窗改为按鼠标位置居中并裁剪到当前显示器工作区。
  - `4198da6` feat(config)：显示设置与热区设置布局、取色器与分组对齐优化。
  - `2deca4a` feat(ui)：托盘版本信息置顶；从配置打开设置时折叠常用文件夹以突出设置区。
  - `1d8a2ca` feat(hotzone)：热区背景图导入、填充 / 对齐、独立透明度；主进程复制图片到应用背景库。
  - `d4236ef` feat(hotzone)：背景图缩放滑条（如 0.5–3）、配置归一化与渲染缩放。
  - `46f3e84` fix(display)：跨屏热区兜底与偏好更新稳定性。
  - `f9519be` feat(display)：**允许跨屏拖动热区**开关（默认关）与风险提示；关闭时拖拽限制在当前屏；修复副屏最左侧拖动时 tab 轨道越界。

## v0.1.3（自 v0.1.2）

- **版本变更**：`0.1.2 -> 0.1.3`（PATCH）。版本号在提交 `f3c2732` 中提升（本发布说明不列出该 bump 提交）。
- **窗口内提交摘要**：
  - `133d9e7` feat(tray)：托盘「更新日志」改为读取 `RELEASE_HISTORY.md`；「版权声明」改为读取 `COPYRIGHT_NOTICE.md`；托盘菜单展示当前版本；将上述文件纳入打包资源；同步调整 `package.json` 与主进程托盘逻辑，并更新助手与版本流程相关文档。

## v0.1.2（自 v0.1.1）

- **版本变更**：`0.1.1 -> 0.1.2`（PATCH）。版本号在提交 `703ddf5` 中与功能改动一并提升（无单独 release bump 提交）。
- **窗口内提交摘要**：
  - `40099cd` fix(overlay)：改进链接拖拽解析（`text/plain`、`text/uri-list`、`text/html` 等），展示区安全打开 `http/https` 链接；配置页补充换行与链接识别相关提示。
  - `703ddf5` fix(quick-open)：修复滚动区域内悬停定时器未清理导致的误下钻；在滚动按钮悬停、列表滚动、目录项离开等路径统一清理 pending hover；**同提交将版本号提升至 0.1.2**。

## v0.1.1（自 v0.1.0）

- **版本变更**：`0.1.0 -> 0.1.1`（PATCH）。版本号在提交 `d212ece` 中提升（本发布说明不重复列出该 bump 提交）。
- **窗口内提交摘要**：
  - `5488bcc` 初始化项目。
  - `ba7e608` 热区透明度迁入配置。
  - `4f85c34` 热区拖拽几何与行为调整。
  - `62aae39` 移除热区 resize 及相关编辑态清理。
  - `a4e86c6` 标题栏布局与拖拽交互优化。
  - `f9c5468` 运行时配置与标题栏颜色。
  - `a4c1c0f` 热区固定与标题栏拖拽冲突处理。
  - `5635745` 标题栏折叠行为。
  - `34fb21a` 展开态缩放与同步。
  - `7f9de3a` 投放成功后可选打开目标文件夹。
  - `fa9dcff` 投放收尾顺序与新建文件夹对话框置顶。
  - `e4a0d46` 多显示器标题栏切换。
  - `5821ddc` 切屏后 overlay 对齐修复。
  - `412990f` 配置重置 y 后 overlay 可见性修复。
  - `779c44a` 展示文本十档字号。
  - `715d393` 投放会话与立即持久化配置。
  - `a93bede` 重启后最小缩放底线修复。
  - `736ed2a` 面包屑建夹与提交竞态修复。
  - `5609eca` 快开模式与配置 / 界面。
  - `17fda7c` 快开悬停查询 + 点击打开。
  - `0b8842e` 快开悬停延迟配置。
  - `108c2be` 快开固定高度与滚动区布局。
  - `875fcbe` 悬停查询延迟与冷却配置重构。
  - `8bd5217` 目录面板投放进度 / 结果展示。
  - `6d04cf4` 开机自启与失败提示。
  - `4e1ae72` 配置分区、调试日志与失败日志。
  - `5a6eaa8` 投放结果按完成时间排序。
  - `a32672a` 失败原因中文化与日志。
  - `9b22b90` 打开配置页重置视图。
  - `70b471e` 编辑态禁用尺寸拖拽。
  - `5dc81c7` Ctrl+Space 与文本滚动。
  - `872bfbf` 常用文件夹布局收紧。
  - `acdee77` 外置标签页与删除交互。
  - `001f60a` 拖拽文本换行与长度上限配置。
  - `8488ddd` 标题栏窄宽折叠与按钮居中。

## v0.1.2（自 v0.1.1）

- **版本变更**：`0.1.1 -> 0.1.2`（PATCH）。版本号在提交 `703ddf5` 中与功能改动一并提升（无单独 release bump 提交）。
- **窗口内提交摘要**：
  - `40099cd` fix(overlay)：改进链接拖拽解析（`text/plain`、`text/uri-list`、`text/html` 等），展示区安全打开 `http/https` 链接；配置页补充换行与链接识别相关提示。
  - `703ddf5` fix(quick-open)：修复滚动区域内悬停定时器未清理导致的误下钻；在滚动按钮悬停、列表滚动、目录项离开等路径统一清理 pending hover；**同提交将版本号提升至 0.1.2**。

## v0.1.3（自 v0.1.2）

- **版本变更**：`0.1.2 -> 0.1.3`（PATCH）。版本号在提交 `f3c2732` 中提升（本发布说明不列出该 bump 提交）。
- **窗口内提交摘要**：
  - `133d9e7` feat(tray)：托盘「更新日志」改为读取 `RELEASE_HISTORY.md`；「版权声明」改为读取 `COPYRIGHT_NOTICE.md`；托盘菜单展示当前版本；将上述文件纳入打包资源；同步调整 `package.json` 与主进程托盘逻辑，并更新助手与版本流程相关文档。

## v0.1.6（自 v0.1.3）

- **版本变更**：`0.1.3 -> 0.1.6`（MINOR）。文档化功能窗口止于 `f9519be`；`package.json` 在 v0.1.7 发布时一并从 `0.1.3` 升至 `0.1.7`。
- **窗口内提交摘要**（不含仅维护 `RELEASE_HISTORY.md` / `COMMIT_HISTORY.md` / `AGENTS.md` 的提交）：
  - `9422abe` fix(overlay)：编辑态拖拽标题时降低 preview IPC 频率，结束时补发一次以同步最终位置。
  - `4649d5e` feat(overlay)：允许热区底边越过屏幕下沿同时保持标题栏可操作；跨屏按钮图标语义优化；编辑文本时 Tab 插入制表符而非切换焦点。
  - `1326dbd` feat(main)：拖拽面板与快开弹窗改为按鼠标位置居中并裁剪到当前显示器工作区。
  - `4198da6` feat(config)：显示设置与热区设置布局、取色器与分组对齐优化。
  - `2deca4a` feat(ui)：托盘版本信息置顶；从配置打开设置时折叠常用文件夹以突出设置区。
  - `1d8a2ca` feat(hotzone)：热区背景图导入、填充 / 对齐、独立透明度；主进程复制图片到应用背景库。
  - `d4236ef` feat(hotzone)：背景图缩放滑条（如 0.5–3）、配置归一化与渲染缩放。
  - `46f3e84` fix(display)：跨屏热区兜底与偏好更新稳定性。
  - `f9519be` feat(display)：**允许跨屏拖动热区**开关（默认关）与风险提示；关闭时拖拽限制在当前屏；修复副屏最左侧拖动时 tab 轨道越界。

## v0.1.7（自 v0.1.6）

- **版本变更**：`0.1.3 -> 0.1.7`（PATCH）。`package.json` 自 `0.1.3` 提升至 `0.1.7`（与文档化 v0.1.6 窗口衔接；本说明不写单独 bump 提交）。
- **窗口内提交摘要**（自 `f9519be` 之后，不含仅维护三份元文档的提交）：
  - `bddad82` 常用文件夹分行省略、拖拽会话与路径修正、单测补充。
  - `869fa65` 快开与面板共用列表/平铺及尺寸配置、配置文案同步。
  - `501f396` 设置顶栏固定、表单区独立滚动。
  - `6f837f5` 常用文件夹拖动手柄排序、边缘滚动与提示、「已修改」/「已删除」提示。

## v0.2.0（自 v0.1.7）

- **版本变更**：`0.1.7 -> 0.2.0`（MINOR）。版本号在提交 `b5ab5db` 中提升（本发布说明不重复列出该 bump 提交）。
- **窗口内提交摘要**（自 `e8350f6` 之后，不含仅维护 `RELEASE_HISTORY.md` / `COMMIT_HISTORY.md` / `AGENTS.md` 的提交）：
  - `5a3a6e9` fix(config)：配置页添加/删除常用文件夹时不重复显示「已修改」。
  - `8200703` feat(config)：热区设置中展示文本加粗与颜色同一行；背景图导入保留安全文件名、选择器默认打开背景库、新增打开背景库文件夹；修复 `AGENTS.md` 正文编码。

## v0.3.0（自 v0.2.0）

- **版本变更**：`0.2.0 -> 0.3.0`（MINOR）。
- **窗口内提交摘要**（自 `b5ab5db` 之后，不含仅维护 `RELEASE_HISTORY.md` / `COMMIT_HISTORY.md` / `AGENTS.md` 的提交）：
  - `f15b29a` feat(config)：热区背景图改为缩略图预览；叠放布局修复与图同显「未选择」；移除选择按钮与打开背景库，点击缩略图选图/替换。

## v0.4.0（自 v0.3.0）

- **版本变更**：`0.3.0 -> 0.4.0`（MINOR）。版本号在发布提交中与 `RELEASE_HISTORY.md` 一并更新（本说明不重复列出单独的版本号提升提交）。
- **窗口内提交摘要**（自 `f15b29a` 之后，不含仅维护 `RELEASE_HISTORY.md` / `COMMIT_HISTORY.md` / `AGENTS.md` 的提交）：
  - `f9f33e1` 热区导出 Word（.docx）、配置目录 `favorite-links` 与 Windows 常用文件夹快捷方式同步、另存为热区所在屏；补充 `docs/user-guide.zh-CN.md`。

## v0.4.1（自 v0.4.0）

- **版本变更**：`0.4.0 -> 0.4.1`（PATCH）。版本号在发布提交中与 `RELEASE_HISTORY.md` 一并更新（本说明不重复列出单独的版本号提升提交）。
- **窗口内提交摘要**（自 `6f28a44` 之后，不含仅维护 `RELEASE_HISTORY.md` / `COMMIT_HISTORY.md` / `AGENTS.md` 的提交）：
  - `c466364` fix(shortcut)：macOS 下模式切换快捷键改为 Control+空格。
  - `47333db` fix(main)：macOS/Linux 托盘使用多倍率图标，逻辑尺寸 16×16。
  - `1dd138a` fix(main)：快开/拖拽面板与新建文件夹窗统一 `screen-saver` 置顶，热区刷新后弹窗仍高于钉住热区。
  - `a3638e2` fix(new-folder)：加高新建文件夹窗口并支持滚动与长路径折行，避免底部按钮被裁切。

## v0.5.1（自 v0.5.0）

- **版本变更**：`0.5.0 -> 0.5.1`（PATCH）。版本号在发布提交中与 `RELEASE_HISTORY.md` 一并更新（本说明不重复列出单独的版本号提升提交）。
- **窗口内提交摘要**（自 `1e428f3` 之后，不含仅维护 `RELEASE_HISTORY.md` / `COMMIT_HISTORY.md` / `AGENTS.md` 的提交）：
  - `023f1f1` fix(main)：首次启动（无 `config.json`）时按主显示器工作区居中初始化热区位置并持久化，避免标题栏溢出屏幕上沿。

## v0.5.0（自 v0.4.1）

- **版本变更**：`0.4.1 -> 0.5.0`（MINOR）。版本号在发布提交中与 `RELEASE_HISTORY.md` 一并更新（本说明不重复列出单独的版本号提升提交）。
- **窗口内提交摘要**（自 `c1029df` 之后，不含仅维护 `RELEASE_HISTORY.md` / `COMMIT_HISTORY.md` / `AGENTS.md` 的提交）：
  - `8fabef7` feat：更新版权声明与用户指南。
  - `ed59830` fix(quick-open)：快开弹窗高度与拖拽弹窗统一。
  - `383d483` chore(build)：打包元数据添加作者信息。
  - `1e428f3` feat(main)：便携版可写数据与 exe 同级 `config` 目录（`PORTABLE_EXECUTABLE_DIR` 优先），日志/背景/常用文件夹快捷方式一并迁入；首次启动可从旧版 `AppData` 下 `dragfree` 目录迁移。
